display_x = 24.8;
display_y = 26.9;
window_x = 23.2;
window_y = 12.2;
pin_to_window_top = 3.5;
pin_to_window_bottom = 7.2;
pin_dist_x = 20.5;
nozzle_dia = 0.4;
side_wall_thickness = nozzle_dia * 2;
front_thickness = 0.8;
back_thickness = 0.8;
print_margin = 0.12;
print_margin_z = 0.05;
extra_space = 0.4;
inner_space = 4.0;
case_height = front_thickness + inner_space + back_thickness;
inner_x = display_x + (print_margin * 2) + (extra_space * 2);
inner_y = display_y + (print_margin * 2) + (extra_space * 2);
top_pin_y = (window_y / 2) + pin_to_window_top;
bottom_pin_y = -(window_y / 2) - pin_to_window_bottom;
pocket_center_y = (top_pin_y + bottom_pin_y) / 2;
outer_x = inner_x + (side_wall_thickness * 2);
outer_y = inner_y + (side_wall_thickness * 2);
recess_depth = 0.24;
pin_height = inner_space - print_margin_z;
pin_dia = 2.4;
flare_height = 1.1;
flare_dia = pin_dia + 1.7;

lid_x = inner_x - (print_margin * 2);
lid_y = inner_y - (print_margin * 2);
cutout_width = 9.8 + (print_margin * 2);
cutout_depth = 4.1 + (print_margin * 2);
recess_dia = 3.3;
recess_pos_x = 1.2;
recess_pos_y = 2.3;
tab_depth = 0.8;
edge_length_factor = 0.75;

filler_extension = 0.4;
filler_height = 1.8;
filler_y_pos = pocket_center_y + (inner_y / 2);
filler_width = cutout_width - print_margin;