display_x = 24.8;
display_y = 26.9;
window_x = 23.4;
window_y = 12.2;
pin_to_window_top = 3.5;
pin_to_window_bottom = 7.2;
pin_dist_x = 20.5;
nozzle_dia = 0.4;
side_wall_th = nozzle_dia * 2;
front_th = 0.8;
back_th = 0.8;
print_margin = 0.14;
eps = 0.01;
extra_space = 0.4;
inner_space = 4.0;
case_height = front_th + inner_space + back_th;
inner_x = display_x + (print_margin * 2) + (extra_space * 2);
inner_y = display_y + (print_margin * 2) + (extra_space * 2);
top_pin_y = (window_y / 2) + pin_to_window_top;
bottom_pin_y = -(window_y / 2) - pin_to_window_bottom;
pocket_center_y = (top_pin_y + bottom_pin_y) / 2;
outer_x = inner_x + (side_wall_th * 2);
outer_y = inner_y + (side_wall_th * 2);
pin_dia = 2.4;
tab_depth = 0.8;
pin_height = inner_space + tab_depth - print_margin;
flare_height = 1.0;
flare_dia = pin_dia + 1.4;
pin_hole_dia = 0.8;
pin_hole_depth = pin_height - flare_height - 0.3;
tab_pocket_width = tab_depth + (print_margin * 2);
pocket_height = tab_depth + print_margin;

lid_clearance = 0.18;
lid_x = outer_x;
lid_y = outer_y;
cutout_depth = 4.1 + (print_margin * 2);
cutout_width = 9.8 + (print_margin * 2);
recess_dia = 3.5;
recess_depth = 0.2;
recess_pos_x = 1.2;
recess_pos_y = 2.3;
edge_length_factor = 0.75;

filler_extension = 0.3;
filler_height = 1.5;
filler_y_pos = pocket_center_y + (inner_y / 2);
filler_width = cutout_width - lid_clearance;
